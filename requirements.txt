# Imprint dependencies (all offline-capable; core app uses only the stdlib).
# Tkinter and sqlite3 ship with Python — nothing to install for those.

python-docx>=1.2.0    # render requirements as an SRS (.docx)
openpyxl>=3.1.0       # traceability matrix + assistant .xlsx output
pypdf>=4.0.0          # read .pdf files for the assistant (OneDrive index)
beautifulsoup4>=4.12.0 # web-search result parsing + .html reading
# docxtpl            # optional later: user-editable .docx templates
